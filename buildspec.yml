version: 0.2

phases:
  install:
    commands:
      - echo "Installing Terraform, tfsec, and checkov"
      - curl -o terraform.zip https://releases.hashicorp.com/terraform/1.9.0/terraform_1.9.0_linux_amd64.zip
      - unzip terraform.zip -d /usr/local/bin
      - curl -s https://raw.githubusercontent.com/aquasecurity/tfsec/master/scripts/install_linux.sh | bash
      - pip install checkov

  pre_build:
    commands:
      - cd "$CODEBUILD_SRC_DIR/terraform" && terraform init -input=false

  build:
    commands:
      - echo "Running terraform plan"
      - cd "$CODEBUILD_SRC_DIR/terraform" && terraform plan -out=tfplan.binary -input=false
      - echo "Running tfsec scan"
      - cd "$CODEBUILD_SRC_DIR/terraform" && tfsec . --format json --out tfsec-report.json || true
      - echo "Running checkov scan"
      - cd "$CODEBUILD_SRC_DIR/terraform" && checkov -d . --output json > checkov-report.json || true

artifacts:
  files:
    - terraform/tfplan.binary
    - terraform/tfsec-report.json
    - terraform/checkov-report.json
  discard-paths: no
